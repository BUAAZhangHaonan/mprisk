# Data

This directory stores versioned data manifests, annotation sheets, prompt banks, and tiny smoke examples.

Raw datasets and generated media are not committed. Put local paths in `configs/local_paths.yaml`.
