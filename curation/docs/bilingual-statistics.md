# Curation statistics

The annotation UI presents queue and annotation progress in English and Chinese. Statistics are served by `/api/progress` and `/api/annotators/{id}/statistics`; the frontend reads these endpoints rather than deriving counts from media files.
