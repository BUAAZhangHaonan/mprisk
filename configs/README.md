# Configs

This directory stores versioned configuration for the paper pipeline.

Local absolute paths belong in `configs/local_paths.yaml`, copied from `local_paths.example.yaml`. That local file is ignored by git.
